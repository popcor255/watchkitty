describe('Tests for the getGitRepos.js', () => {
    test('expect getGitRepos to extract array of git repos from payload', () => {
        let endpoint_response_mock = require('../mocks/pipeline_resources_mocks/resource_with_1_repo.json');
        let { get_git_repo } = require('../../src/utils/get_git_repos.js');
        
    });
})