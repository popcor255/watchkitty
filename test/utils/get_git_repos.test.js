describe('Tests for the getGitRepos.js', () => {
    test('expect getGitRepos to extract array of git repos from payload', () => {
        let git_payload_mock = require('./mocks/pipeline_resources_mocks/resource_with_1_repo.json');
        let { get_git_repo } = require('../src/utils/get_git_repos.json');
        
    });
})