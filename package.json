{
  "name": "watchkitty",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.9",
    "jest": "^25.1.0"
  },
  "scripts": {
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "dev": "node src/index.js",
    "tdd": "jest --watch"
  },
  "dependencies": {
    "dotenv-safe": "^8.2.0",
    "request": "^2.88.0"
  }
}
